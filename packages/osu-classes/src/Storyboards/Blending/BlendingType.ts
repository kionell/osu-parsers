/**
 * Types of blending.
 */
export const enum BlendingType {
  Inherit = 0,
  ConstantAlpha,
  ConstantColor,
  DstAlpha,
  DstColor,
  One,
  OneMinusConstantAlpha,
  OneMinusConstantColor,
  OneMinusDstAlpha,
  OneMinusDstColor,
  OneMinusSrcAlpha,
  OneMinusSrcColor,
  SrcAlpha,
  SrcAlphaSaturate,
  SrcColor,
  Zero,
}

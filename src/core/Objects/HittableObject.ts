import { ConvertHitObject } from './ConvertHitObject';

/**
 * A parsed hittable object.
 * Used only for conversion between different rulesets.
 */
export class HittableObject extends ConvertHitObject {}
